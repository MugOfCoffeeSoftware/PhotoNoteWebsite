---
import Layout from "../../layouts/Layout.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import LegalPage from "../../components/legal/LegalPage.astro";
import PrivacyPolicyEn from "../../components/legal/PrivacyPolicyEn.astro";
import PrivacyPolicyDe from "../../components/legal/PrivacyPolicyDe.astro";
import { getTranslations, locales, type Locale } from "../../i18n";

export function getStaticPaths() {
  return [
    { params: { lang: "en" } },
    { params: { lang: "de" } },
  ];
}

const { lang } = Astro.params as { lang: Locale };
if (!locales.includes(lang)) {
  throw new Error(`Unknown locale: ${lang}`);
}
const t = getTranslations(lang);
---

<Layout
  lang={lang}
  title={t.privacyPolicy.title}
  description={t.privacyPolicy.description}
  path="privacy-policy"
>
  <Header lang={lang} t={t.header} />
  <LegalPage>
    {lang === "de" ? <PrivacyPolicyDe /> : <PrivacyPolicyEn />}
  </LegalPage>
  <Footer lang={lang} t={t.footer} />
</Layout>
